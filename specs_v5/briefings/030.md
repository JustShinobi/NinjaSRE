# Briefing entregue ao spec-implementer — specs_v5/030-setup-wizard

Registrado literalmente para confronto. Este é o texto que o subagent recebeu.

---

Implemente a feature `/srv/workspaces/NinjaSRE/specs_v5/030-setup-wizard` de
ponta a ponta.

**Leia esta seção antes de qualquer outra coisa.** A `spec.md` da 030 foi escrita
em 2026-08-14 descrevendo uma tela que **não existe mais**. A 001 já reconstruiu
o first run como wizard: o stepper existe, os sete passos existem, o modelo de
passos existe, a fonte única de contagem existe e o convite do dashboard existe.
Se você ler a spec e sair construindo, você vai construir pela segunda vez o que
já está lá — e um segundo modelo de passos é exatamente a fonte dupla que esta
feature existe para matar.

**A sua feature é o delta.** Eu levantei o delta antes de te chamar e ele está
abaixo, com `file:line`. Confira cada afirmação minha contra o disco: se eu
estiver errado em alguma, a árvore vence e eu quero saber.

## Estado da árvore quando você começa

HEAD `058aa91` no branch `master`, working tree **limpa**. A 001, a 010 e a 020
desta onda já estão entregues, confrontadas e commitadas.

**Um vermelho é esperado e não é seu.** O operador decidiu, deliberadamente, não
aceitar 23 baselines visuais divergentes até que 040/050/060 construam as telas
de verdade por trás delas. Consequência permanente enquanto durar:

- `make verify` termina **EXIT=2 na etapa visual**. Todas as etapas anteriores
  passam. Isso é o estado esperado, não regressão a caçar. **Atenção:**
  `make verify` é `... console-check test` e o `console-check` morre na etapa
  visual, então **`make verify` nunca chega no pytest**. Enquanto os 23 estiverem
  abertos, `make verify` não dá sinal nenhum sobre a suíte Python — rode o pytest
  separado.
- `uv run pytest -q` termina com **exatamente 1 falha real**:
  `tests/contract/console/test_console_visual_regression.py::test_the_untouched_baselines_still_match`.
- Se aparecer uma **segunda** falha,
  `tests/architecture/test_one_fictional_deployment.py`, ela é artefato de
  `console/test-results/` e `console/playwright-report/` deixados por uma rodada
  de Playwright. `rm -rf` nos dois e ela passa (confirmado quatro vezes).

Qualquer vermelho **além desses** é seu até você provar o contrário com o texto
do erro.

**A armadilha que já mordeu três agentes desta onda, incluindo o orquestrador.**
A regra não é sobre um módulo: **nenhum módulo de teste que escreve na árvore
viva roda em paralelo com qualquer outra coisa.** Dois são conhecidos:

- `tests/contract/console/test_console_visual_regression.py` escreve um PNG em
  branco por cima de um baseline commitado (alfabeticamente
  `administration-1440-light.png`) e restaura num `finally`;
- `tests/contract/console/test_console_gate.py` semeia fixtures de falha
  (`console/tests/unit/seeded.test.ts`, `console/tests/e2e/seeded.spec.ts`) e as
  remove num `finally`.

Os dois são seguros sozinhos e perigosos concorrentes. **Uma rodada de pytest
morta no meio — timeout curto, Ctrl-C, OOM — deixa o estrago na árvore**, e
parece defeito de alguém. Não rode pytest nesses módulos com timeout apertado, e
nunca rode dois pytest ao mesmo tempo. Se `git status` mostrar aquele PNG
modificado ou um `seeded.*` sobrando, **me diga; não conserte** — restaurar é
`git checkout` de arquivo, que você não faz. E antes de apagar qualquer
`seeded.*` que sobrou, cheque `pgrep -af pytest`: pode ser de uma rodada viva.

**Baseline que eu medi imediatamente antes de te chamar:**
`console/tests/unit/surfaces/first-run.test.tsx` +
`console/tests/unit/surfaces/first-run-plan.test.ts` — **94 passed**, 3.46s.

## O que já existe, e que você verifica em vez de construir

Cada linha abaixo eu abri. Rode os testes desses arquivos **antes** de mudá-los.
Linha de ledger já satisfeita é **verificada**, e o relatório diz "já valia,
verificado em `file:line`" — nunca a apresenta como conserto.

### O modelo de passos — `console/src/surfaces/first-run/plan.ts`

Este módulo **já é** o que a T003 pede. Não crie um `steps.ts` ao lado.

- `WIZARD_STEPS` (`plan.ts:24`) — os sete do mockup-4, em ordem: `provider`,
  `credential`, `model`, `integrations`, `verify`, `estate`, `alerts`.
- `stepDone` (`plan.ts:152`) — o estado de cada passo, com `setup.complete`
  vencendo tudo.
- `outstanding` (`plan.ts:199`) — **a fonte única da FR-002**. Lê
  `setup.steps`, o array que `GET /v1/setup/checklist` serve, e não os sete da
  UI. O docstring explica por quê e um teste de contrato o segura (abaixo).
- `currentStep` (`plan.ts:211`) e `hrefFor` (`plan.ts:221`) — o passo atual vem
  do endereço (`?step=`), então reabrir passo concluído já é navegação, e a
  **navegação para trás da FR-001 já existe**.
- `planFor` (`plan.ts:234`) — as sete linhas com `done`/`current`/`href`.
- `readSetup` (`plan.ts:255`) — monta `DeploymentSetup` do checklist mais a
  configuração efetiva.
- `SOURCE_STEP` / `INVESTIGATION_STEP` / `RUNTIME_STEP` (`plan.ts:75,78,91`) —
  os nomes que o **backend** dá aos passos de checklist. Os sete da UI e os do
  backend são vocabulários diferentes e essa distinção é deliberada.

### O stepper — `console/src/surfaces/screens/first-run.tsx`

- A lista dos sete (`:371-423`), cada um um `Link` (`:388`) com
  `data-testid="wizard-step"`, `data-step`, `data-done`, `data-current`; o atual
  destacado com `bg-sunken` e um "You are here" (`:413-420`).
- O rótulo de "continua em `<tela>`" ao lado do passo (`:402-412`), via
  `handoverScreen` (`:92`), que lê o nome da tela de `areaByPath` — a **FR-003
  já tem metade**: o passo diz para onde leva, antes de você o abrir.
- O corpo do passo atual (`:469-726`), um bloco por passo.
- O `handover` (`:698-725`) com link para `HANDOVER[here]` (`:77-80`:
  `estate → /resources`, `alerts → /signals`).
- `setup-complete` (`:676-696`) — o resumo de conclusão **já existe em parte**.

### A verificação — `console/src/surfaces/first-run/verify.tsx`

- Uma linha por item, cada uma com botão próprio e retry próprio (`:139-199`).
- `StatusChip` por linha (`:148`), com a leitura correta: sem veredito, mostra
  `thing.readiness` — a palavra que o **deployment** reporta —, e só depois do
  teste vira `verified`/`failing` (`:150-156`). **O "lê 'Stored', não 'Nobody has
  checked this one'" da US2 AC1 provavelmente já vale**; confirme contra
  `credentialStatus` (`console/src/design/status.ts`) e diga onde conferiu.
- `remedy` e `findings` já vêm do backend e já são exibidos (`:176-196`).

### O convite do dashboard — `console/src/surfaces/setup-hero.tsx`

Já consome `outstanding`/`planFor`/`currentStep`/`hrefFor` e **já some sozinho**
quando não sobra nada (`setup-hero.tsx:43`). A FR-007 "o convite some" e a US3
AC2 "o convite volta" são propriedades deste componente, e ele já as tem: o
`return null` é condicionado ao mesmo `outstanding` que tudo o mais lê. Verifique
com teste em vez de reescrever.

Outros consumidores da mesma fonte, que provam a SC-002 se você não os quebrar:
`console/src/surfaces/emptiness.ts:47`, `console/src/surfaces/screens/dashboard.tsx:275`,
`console/src/shell/load.ts:274`.

## O delta — o que é realmente seu

### 1. A contagem dupla, viva, no mesmo painel (FR-002, SC-002)

**Este é o defeito central da feature e ele está no disco agora.** No mesmo
painel de checklist há duas contagens, de duas fontes, com dois denominadores:

- **título**, `first-run.tsx:322-325`:
  `checklistTitle(plan.filter((entry) => entry.done).length, WIZARD_STEPS.length)`
  — um tally **7-based, client-only**, exatamente o que o teste de contrato
  proíbe;
- **corpo**, `first-run.tsx:365-370`:
  `firstRun.progress` com `left: outstanding(setup)` e
  `total: setup.steps.length` — a **fonte única**, servida pelo backend.

`setup.steps` **não tem sete elementos**: são os passos do checklist do backend,
outro vocabulário e outra cardinalidade. As duas linhas discordam por
construção. É literalmente o "'2 of 7' e '3 outstanding' simultâneos" que a
SC-002 nomeia como o estado a matar, e ele sobreviveu à 001.

`tests/contract/console/test_console_first_run.py:181`
(`test_the_progress_count_is_not_recomputed_from_the_console_s_own_wizard_steps`)
existe e passa — mas ele inspeciona **só o corpo da função `outstanding`** por
regex. O título escapa dele por estar em outro arquivo. **A T002 é estender esse
teste para pegar o que ele não pega**, e o vermelho tem de ser visto contra o
código de hoje.

Decida e me diga: ou o título passa a ler a fonte única, ou a contagem some do
título e fica só no corpo. Uma das duas, não as duas.

### 2. "Passo N de 7 — <nome>" (FR-001, US1 AC1)

Não existe. Hoje o subtítulo é `firstRun.progress` = `'{left} of {total} steps
left'` (`console/src/i18n/en.ts:483`), que é quantos **faltam**, não **onde você
está**. A US1 AC1 pede a posição. Note que "de 7" é o denominador **da UI**, não
o do checklist — então esta frase e a contagem da fonte única falam de coisas
diferentes e ambas podem existir sem se contradizer, **desde que digam
explicitamente o que contam**. Escreva as duas de forma que um operador não as
leia como a mesma medida discordando de si mesma.

### 3. O retorno ao wizard (FR-003, US1 AC2, T006)

O passo diz para onde leva; **a tela de destino não oferece volta**. Não existe
parâmetro de retorno, não existe banner "Continuar o setup". É construção sua,
nas duas telas de destino.

**Fato que a spec não sabia:** a 010 aposentou `/signals`.
`console/src/shell/routes.ts:701` redireciona `/signals → /settings/alert-intake`.
Mas `HANDOVER.alerts` (`first-run.tsx:79`) ainda aponta para `/signals`, ou seja,
o handover do último passo hoje atravessa um redirect. Os destinos reais são
**`/resources`** (`routes.ts:177-178`, área `resources`) e
**`/settings/alert-intake`** (`routes.ts:600-601`, área `settings-alert-intake`).
Decida se `HANDOVER` passa a nomear o endereço final e me diga o que decidiu — um
link que sobrevive por redirect é um link que a próxima limpeza quebra.

`areaByPath` (`routes.ts:372`) casa **exato** e `resolveCta`
(`console/src/design/empty-state.ts:38`) **levanta** para rota fora do manifesto.
O parâmetro de retorno é query string, não segmento novo.

### 4. Diagnóstico com correção a um clique (FR-005, US2 AC2, SC-003)

`verify.tsx:173-180` mostra `detail` e `remedy` como **texto**. Não há link, não
há CTA, não há expansão. Falta:

- limitar o diagnóstico visível a duas frases (FR-005 diz "até duas");
- o **resto** atrás de uma expansão — `Reference`
  (`console/src/design/reference.tsx:29`) é a forma que a 001 deixou pronta para
  "resumo visível, resto atrás de um controle";
- a **ação nomeada** que aterrissa no campo que corrige, pelo contrato de CTA.

**Onde a correção de modelo aterrissa hoje:** a 040 (Models & providers) **não
existe ainda** — a `spec.md` diz "destino transitório: telas atuais; final: 040".
O destino transitório honesto é `/first-run?step=model`, que é `hrefFor('model')`
e já é o campo que corrige. Use `hrefFor`, não uma string literal, para que a 040
mude um lugar só. Se você achar que outro destino é mais honesto, diga qual e por
quê antes de implementar.

### 5. "Continuar mesmo assim" e o bloqueio por passo (FR-006, US2 AC4, T009)

**Ausente por inteiro.** Nada na árvore declara qual passo é bloqueante. A
FR-006 diz que o bloqueante "é declarado **por passo**" — então a declaração é
dado do modelo de passos (`plan.ts`), ao lado de `WIZARD_STEPS`, não um `if`
espalhado na tela. O exemplo que a própria FR-006 dá: sem provider de modelo é
bloqueante.

"com a pendência anotada no dashboard" (US2 AC4) é a segunda oração da frase e é
requisito próprio — **enumere-a como linha separada do ledger**. Nesta onda, duas
features seguidas marcaram FEITO um requisito cuja segunda oração não existia no
código. É o padrão mais caro da onda e ele reincidiu.

### 6. A rota que redireciona quando o setup acaba (FR-007, US3 AC1, T011)

A 010 **deixou isto explicitamente para você**. `console/src/shell/routes.ts:686`
diz, em tantas palavras:

> `/first-run` is not here: its redirect depends on whether the checklist is
> complete, a fact this table cannot hold, so its own route file reads it
> directly.

Ou seja: **não** entre em `SETTINGS_REDIRECTS`. O redirect mora em
`console/src/app/(shell)/first-run/page.tsx`, que já lê o checklist. O padrão de
redirect da 010 está em `console/src/shell/legacy-redirect.ts` — leia antes de
inventar um segundo mecanismo.

O resumo de conclusão existe em parte (`first-run.tsx:676-696`); a FR-007 pede
que ele **resuma o que ficou configurado, com nomes de exibição** — a lista
`established` (`first-run.tsx:291-315`) já tem exatamente esses nomes. Reaproveite.

### 7. Os três painéis (FR-008)

A FR-008 manda substituir "What is left / What is set up so far / Check that each
of them works". A 001 já mudou essa tela: hoje são o painel de passos
(`:353-424`), o painel `established` (`:429-453`) e o corpo do passo atual
(`:457-727`) — e o terceiro **não** é mais um painel de estado duplicado, é o
wizard. **Julgue com o código na frente**: qual estado ainda aparece duas vezes
em vocabulários diferentes? A contagem dupla do item 1 é um caso certo. O
`established` é candidato — ele mostra readiness que o stepper também insinua.
Decida, justifique no relatório, e não apague painel só porque a spec cita um
título que a tela não usa mais.

## Onde os testes moram — os runners deste repositório só coletam isto

- **vitest** coleta **só** `console/tests/unit/**/*.test.ts(x)`. Nada dentro de
  `console/src/` roda. `console/tests/unit/surfaces/first-run.test.tsx` e
  `first-run-plan.test.ts` **já existem** (94 testes, verdes) — estenda, não
  substitua.
- **behaviour**: `console/tests/e2e/` (`console/tests/e2e/setup-wizard.spec.ts` é
  novo, T004/T007).
- **first-day**: `console/tests/first-day/first-day.spec.ts` — projeto Playwright
  com **cenário de deployment vazio**. É o projeto que exercita exatamente a tela
  desta feature no estado limpo. Leia-o antes de escrever a T004: parte da US1
  AC1 pode já estar coberta ali, e **um caso da 010 custou caro por alguém não
  ter lido este arquivo**.
- **visual**: `console/tests/visual/screens.spec.ts`.
- O `use.viewport` global do Playwright é **1440×900**. Medição em 1080p declara
  o próprio viewport, explicitamente, lendo a constante.
- **Contrato Python**: `tests/contract/console/test_console_first_run.py` (T002
  estende). 337 linhas, 11 testes; leia todos antes de escrever o seu, porque
  vários já provam parte do que você ia provar de novo.

### As fixtures dos três estados (T001)

**Ache antes de construir.** `fixtures/scenarios/` já tem cinco cenários:
`empty`, `first-run`, `incident-live`, `populated`, `restricted`. Os três estados
que a T001 pede — limpo, meio, completo — muito provavelmente já são `empty`,
`first-run` e `populated`. Confira o que cada um serve para
`/v1/setup/checklist` (`tools/mockplane/dataset/served.py`,
`tools/mockplane/endpoints.py`) e **reporte quais já valiam**. Se faltar um
estado, o cenário é construído pelo **builder** (`tools/mockplane/dataset/build.py`)
— fixture **nunca** é editada à mão: `test_only_the_builder_writes_a_fixture_file`
reprova.

## Escopo, e o que não é seu

- **`/configuration` é da 070.** Não toque no editor cru.
- **Não construa as telas da 040/050/060.** A 040 dá o destino final da correção
  de modelo; até lá o destino é `hrefFor('model')`.
- Não toque em diretório de spec de outra feature, nem em
  `tests/unit/tools/test_console_smoke.py`, nem em
  `tests/contract/integrations/test_integration_parity.py`.
- **As 23 divergências visuais abertas não são suas.** Não rode
  `make console-visual-accept` nem `console-e2e`. Teste visual
  (`--project=visual`) é comparação, não aceitação. Se precisar registrar tela
  nova em `console/visual/screens.json`: adicione a entrada e **reporte o
  baseline como ausente, pelo nome** — `python -m tools.console_gate e2e` escreve
  um PNG placeholder para toda tela declarada sem baseline e o teste de cobertura
  só confere que o arquivo existe. Onze PNGs byte-idênticos já passaram por
  captura assim. **Apague o que o gate escrever.**

## A definição de pronto

`specs_v5/mockups/settings-v5.html#mockup-4` — stepper de sete passos, chips
canônicos por verificação, falha com correção nomeada, "Continuar mesmo assim".
Normativo em layout, hierarquia, agrupamento e vocabulário; pixel exato não.

## Regras que não se negociam

- **Nunca** `git add`, `git commit`, `git stash`, `git checkout` de arquivo. Sua
  entrega é a árvore de trabalho suja. Eu commito.
- **Test-first com o vermelho visto.** O teste que falha entra antes da
  implementação e você **roda e vê** o vermelho. Inferir que "teria falhado" não
  é confirmação e não pode ser relatado como uma. Se em algum item você não
  conseguiu ver o vermelho antes, diga isso **nessas palavras**, item por item.
- **Vírgula em requisito é fronteira de requisito.** Enumere cada oração de cada
  FR e de cada cenário como linha própria do ledger **antes** de escrever código.
  Duas features desta onda marcaram FEITO um requisito cuja segunda oração não
  existia no código — é o padrão mais caro da onda, e reincidiu.
- **Ache o que já existe antes de construir.** Metade desta feature já está no
  disco. O relatório diz "já valia, verificado em `file:line`".
- **Persiga a causa raiz entre tiers.** Defeito visível numa tela é rotineiramente
  produzido em `gateway/`, em `platform/`, ou no plano de mock.
- Todo texto novo entra em `console/src/i18n/en.ts` **e** `pt-BR.ts`. O pt-BR é
  **brasileiro**: tela, arquivo, ação, salvar, excluir, ativar, contatar. Nunca
  ecrã, ficheiro, acção, gravar, apagar, activar, contactar.
- **Nenhum literal de cor**; cor por papel (`text-text`, `bg-surface`,
  `text-accent`, `success`/`warning`/`danger`/`info`/`neutral`), e cor nunca é o
  único portador.
- **Estado de tela mora na URL.** O passo atual já mora (`?step=`); o parâmetro de
  retorno também.
- **Toda constante mora em `config/constants/`**, no módulo do domínio que a
  possui. Limite, page size, orçamento: **lido**, nunca escolhido.
- **Ausente, não desabilitado.** **Dependência ausente quebra o painel, nunca a
  rota.** `exactOptionalPropertyTypes` está ligado: opcional que pode faltar é
  `foo?: T | undefined`.
- **Segredo é write-only.** Campo de credencial posta uma vez e nunca é
  renderizado de volta, nem mascarado.
- Todo valor de enum e string de status que o console renderiza é conferido contra
  o port ou enum que o declara em `platform/` ou `core/` — nunca contra
  `fixtures/` nem `tools/mockplane/`. Quando discordarem, **a declaração vence**.
- **`console/src/shell/routes.ts` é a única lista do que existe como rota**, e cada
  entrada carrega a permissão que o *gateway* exige, **copiada pelo nome**.
  `tests/contract/console/test_console_shell.py` segura uma contra a outra.
- Nenhum identificador de requisito (`FR-001`), critério (`SC-002`), artigo da
  constituição, número de feature ou caminho de `specs_v5/` em arquivo commitado
  — código, teste, comentário, `AGENTS.md`. Todo diretório de onda é gitignored:
  quem clona o repositório não tem esses documentos. **Diga a substância.**
- Nunca nomeie projeto upstream. Nunca `Co-Authored-By:` nem rodapé de "generated
  with".

## O que eu vou fazer com o seu relatório

Nada, até conferir. Vou abrir cada `file:line` que você citar, rodar de novo cada
gate que você disser ter rodado, e rodar os testes que você disser ter escrito.
Depois um verificador independente, em contexto novo, refaz o trajeto. Onde a
árvore discordar de você, a discordância vai para `specs_v5/CONFRONTO.md` com as
duas versões.

No relatório final, para **cada** linha do ledger — FR-001..FR-008, cada cenário
de aceitação das três user stories, cada edge case, SC-001..SC-004, T001..T013:

1. estado final e o `file:line` que prova;
2. todo arquivo criado ou mudado;
3. todo gate rodado e seu resultado **real** — gate não rodado é listado como não
   rodado, com o porquê; gate que falhou é listado como falhou, com o texto do
   erro;
4. toda linha que você deliberadamente deixou, e qual spec a possui;
5. tudo que a spec, o plano ou o mockup não responderam, e o que você assumiu —
   **incluindo cada ponto onde a spec descreve uma tela que a 001 já substituiu.**

A T013 termina escrevendo `specs_v5/030-setup-wizard/controle.md`, que diz **só o
que o código prova**.

**Subestime em vez de superestimar**: uma pendência nomeada me custa muito menos
que uma conclusão que não se sustenta.
