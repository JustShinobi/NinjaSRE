# Briefing entregue ao spec-implementer — specs_v5/001-fundacao-vocabulario-ux

Registrado literalmente para confronto. Este é o texto que o subagent recebeu.

---

Implemente a feature `/srv/workspaces/NinjaSRE/specs_v5/001-fundacao-vocabulario-ux`
de ponta a ponta. Ela é a fundação da onda: 010–070 consomem o que você deixar.

## Estado da árvore quando você começa

`make verify` completo foi rodado nesta árvore imediatamente antes de você
começar — lint, format-check, typecheck, os 7 contratos de import (7 mantidos,
0 quebrados), todos os guard checks, as 85 integrações em paridade total, e
`console-check` inteiro (prettier, eslint, tsc, **1945 testes vitest em 125
arquivos** com cobertura ≥90%, build, client-check, budget, e2e behaviour,
first-day e visual). Tudo verde.

O pytest tinha **3 falhas anteriores à onda**, que eu corrigi antes de te
chamar (paginação de `google_gemini`; `/catalogue` fantasma em dois testes de
smoke do console). O número contra o qual você é medido é
**14031+ passed, 0 failed, 25 skipped**.

**Qualquer vermelho que aparecer depois é seu até você provar o contrário com o
texto do erro.** Se achar que uma falha é anterior, o log do baseline está em
`scratchpad/gates/` e o dossiê em `specs_v5/CONFRONTO.md` §0 lista exatamente
o que já estava quebrado — nada além daquelas três estava.

HEAD: `6fb1525`. Working tree: ` M .gitignore` mais os dois arquivos de teste
que corrigi (`tests/unit/tools/test_console_smoke.py`,
`tests/contract/integrations/test_integration_parity.py`). **Não mexa nesses
dois** — não são seus.

## Os caminhos de teste já foram corrigidos — leia a nota do README

O `tasks.md` desta feature foi escrito citando caminhos que os runners deste
repositório **não coletam**. Eu corrigi as specs antes de te chamar. A seção
"Onde os testes moram" do `specs_v5/README.md` tem os fatos; o resumo:

- vitest coleta só `console/tests/unit/**` — nada dentro de `console/src/`.
  `console/tests/unit/design/status.test.ts` **já existe**: estenda.
- Playwright behaviour é `console/tests/e2e/`; `console/e2e/` não existe.
- O viewport global do Playwright é `1440×900`. A medição de 1080p do T018
  declara o próprio viewport, a partir da constante nomeada do T001.

Se você encontrar mais algum caminho que não bate com a árvore, **pare, me
diga qual, e siga pela árvore** — não crie o diretório que a spec inventou.

Também já anotei no `tasks.md`, direto nas tasks, três fatos da árvore que você
teria que descobrir sozinho e que decidem a forma da entrega:

- **T008/T009** — o nome de exibição é declarado em `IntegrationProfile`, no
  pacote do próprio vendor. Um mapa central `id → nome` **quebra** o desenho
  que o módulo declara em prosa ("adicionar uma integração edita zero arquivos
  existentes"). E `IntegrationProfile.__post_init__` já levanta para `summary`
  em branco — é ali que um `display_name` em branco deve morrer também.
- **T017** — a recomputação local a matar tem endereço:
  `console/src/surfaces/first-run/plan.ts:188`.

Leia as tasks: elas agora carregam os `file:line`.

## O que é a definição de pronto

`specs_v5/mockups/settings-v5.html`, seção **"Regras transversais para a
spec"** (Parte 3) — é a expressão visual desta feature. Os quatro mockups
mostram o vocabulário, os nomes de exibição e os CTAs em uso; a Parte 3 é a
regra. Repare que a regra de vocabulário do mockup enumera **quatro** palavras
para credencial (Não conectada · Armazenada · Verificada · Falhando) e a
FR-001 acrescenta o degradado **Unknown** — são cinco chaves no módulo, quatro
estados de credencial mais o degradado. O `tasks.md` (T003) já diz cinco.

## Escopo, e o que não é seu

- FR-001 diz "consumido por todas as superfícies (console, CLI, notificações)"
  e o `tasks.md` só cobre o console. Faça o console, e **nomeie no
  `controle.md`** que CLI e notificações ficaram, dizendo onde estão os
  consumidores que sobraram.
- FR-011 ("ajuda mora no campo", nada de subtítulo de prosa sob título de
  página) é regra desta fundação, mas aplicá-la às telas de 040/050/060 é
  daquelas features. Aqui: deixe o mecanismo pronto e aplique onde a fundação
  já toca.
- T018 já prevê que as telas grandes de hoje **falhem** o orçamento até
  020/070 existirem. O teste é o instrumento de medição da onda: ele nasce
  medindo e marcando o que está fora, não maquiando o número para passar.

## Regras que não se negociam

- **Nunca** `git add`, `git commit`, `git stash`. Sua entrega é a árvore de
  trabalho suja. Eu commito.
- Não rode `make console-visual-accept`, `console-e2e` nem nada que reescreva
  baseline commitado — nenhuma task sua pede isso.
- Se você adicionar tela a `console/visual/screens.json`, **não** deixe o gate
  fabricar o baseline: ou capture deliberadamente porque uma task pede, ou
  apague o que o gate escreveu e reporte o baseline como ausente, pelo nome.
- Todo texto novo entra em `console/src/i18n/en.ts` **e** `pt-BR.ts`. pt-BR é
  brasileiro: tela, arquivo, ação, salvar, excluir, ativar, contatar.
- Nenhum identificador de requisito (`FR-001`), critério (`SC-002`), artigo da
  constituição, número de feature ou caminho de `specs_v5/` em arquivo
  commitado — código, teste, comentário, `AGENTS.md`. Diga a substância.
- Test-first com o vermelho **visto**. Se em algum item você não conseguiu ver
  o vermelho antes, diga isso nessas palavras, item por item.

## O que eu vou fazer com o seu relatório

Nada, até conferir. Vou abrir cada `file:line` que você citar, rodar de novo
cada gate que você disser ter rodado, e rodar os testes que você disser ter
escrito. Onde a árvore discordar de você, a discordância vai para um dossiê de
confronto com as duas versões. Subestime em vez de superestimar: uma
pendência nomeada me custa muito menos que uma conclusão que não se sustenta.

No relatório final, para **cada** linha do ledger (todo FR-001..FR-011, todo
cenário de aceitação, todo edge case, todo SC-001..SC-005, todo T001..T019):
estado final e o `file:line` que prova. E a lista de todo arquivo que você
criou ou mudou.
