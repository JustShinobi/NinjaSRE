# Controle — Setup como wizard com contagem única

O estado abaixo foi verificado contra o código em disco em 2026-08-15, com HEAD
`058aa91` em `master`. Cada linha foi conferida abrindo o `file:line` citado ou
rodando o teste citado — não é uma leitura da especificação.

## 1. Ledger — Requisitos Funcionais

| Peça | Estado | Detalhe |
|---|---|---|
| FR-001, cláusula "estado por passo (concluído, atual, pendente)" | FEITO (já existia, verificado) | `console/src/surfaces/screens/first-run.tsx:415-464` (`plan.map`, `StatusDot`, destaque `bg-sunken` do atual). Provado por `console/tests/unit/surfaces/first-run.test.tsx:118` ("keeps every step visible, marking exactly one as where you are") e `:198` ("marks the current step 'you are here'"). |
| FR-001, cláusula "navegação para trás" | FEITO (já existia, verificado) | Cada passo é um `Link` (`first-run.tsx:426`). Provado por `first-run.test.tsx:176` ("reopens a completed step from its own address") e `:182` ("makes every step a link..."). |
| US1 AC1, "Passo N de 7 — \<nome\>" | FEITO (novo) | `first-run.tsx:377-390` (`data-testid="wizard-position"`), chaves `firstRun.wizard.position` em `console/src/i18n/en.ts:476-481` e `pt-BR.ts:75`. Vermelho visto em `first-run.test.tsx` (testes "says which of the seven this is..." e "moves the position...", linhas ~213-231) antes da implementação; verde depois. Também provado ao vivo contra o cenário `first-run` em `console/tests/first-day/first-day.spec.ts:260-274` ("names the position by number and by name..."). |
| FR-002 | FEITO (conserto real) | **Decisão: o título passou a ler a fonte única — a contagem não saiu do título, ela foi corrigida para citar os mesmos dois números do corpo.** `checklistTitle` (`console/src/surfaces/screens/first-run-heading.ts`) manteve sua assinatura `(esquerda, total)`; o que mudou foi a chamada em `first-run.tsx:349-352`, de `checklistTitle(plan.filter(done).length, WIZARD_STEPS.length)` (tally de 7, cliente) para `checklistTitle(setup.steps.length - outstanding(setup), setup.steps.length)` — exatamente `outstanding(setup)` e `setup.steps.length`, os dois números que a linha de progresso do corpo (`first-run.tsx:398-403`) já lia. Vermelho visto: `tests/contract/console/test_console_first_run.py::test_the_checklist_heading_is_not_a_second_tally_beside_outstanding` (linha 206) falhou antes do conserto (`assert 'WIZARD_STEPS' not in call` — a chamada continha `WIZARD_STEPS.length`); passa depois. `firstRun.progress` (corpo) e o rótulo do título agora citam os mesmos dois números — não há mais denominadores diferentes (7 da UI vs. 5 do checklist) decidindo coisas diferentes no mesmo painel. |
| FR-003, cláusula "leva à tela com um parâmetro de retorno" | FEITO (novo) | **Decisão: `HANDOVER` passou a nomear `/settings/alert-intake` diretamente, não `/signals`.** `console/src/surfaces/screens/first-run.tsx:88-97` (`HANDOVER`, `handoverHref`); `console/src/surfaces/first-run/return-banner.tsx:19-27` (`SETUP_RETURN_PARAM`, `withSetupReturn`). Motivo registrado no comentário de `first-run.tsx:82-87`: `/signals` bare já atravessa o redirect da 010 (`routes.ts:696-705`) até `/settings/alert-intake` hoje mesmo — apontar direto ao endereço final não muda o comportamento agora, mas sobrevive à remoção futura da tabela de redirects, que um link para `/signals` não sobreviveria. Isso exigiu estender `handoverScreen` (`first-run.tsx:106-119`) para consultar `settingsPageByPath` como alternativa quando `areaByPath` não encontra nada (`/settings/alert-intake` está em `SETTINGS_PAGES`, não em `AREAS`). Vermelho visto em `first-run.test.tsx` (asserção `expect(query).toBe('return=setup')` e `toHaveTextContent('Alert intake')`, linhas ~1244-1296) antes do conserto; verde depois. |
| FR-003, cláusula "a tela de destino DEVE oferecer o retorno... enquanto o setup estiver incompleto" | FEITO (novo) | `console/src/surfaces/first-run/return-banner.tsx` (`SetupReturnBanner`, linha 51) — absent quando `!requested \|\| outstanding(setup) === 0`. Ligado em `console/src/surfaces/screens/resources.tsx:31-32,158-165,187` (novo `readSetupState`/`requestedSetupReturn` no `Promise.all` + render) e em `console/src/surfaces/screens/settings-not-built.tsx:8-9,29-41` (função virou `async`, mesmo padrão). Vermelho visto em `console/tests/unit/surfaces/resources.test.tsx` (describe "the way back to the wizard", linha ~371) e em `console/tests/unit/surfaces/settings-not-built.test.tsx` (novo arquivo) antes da implementação; verde depois. Provado ao vivo em `console/tests/first-day/first-day.spec.ts:276-296` (ida a `/resources?return=setup`, banner visível, clique volta ao wizard) e a ausência do banner quando completo em `console/tests/e2e/setup-wizard.spec.ts:39-48` (cenário `populated`, ambos os destinos). |
| FR-004 (chip canônico, teste individual, re-teste) | FEITO (já existia, verificado) | `console/src/surfaces/first-run/verify.tsx:148-176` (linha por item, `Button` de teste próprio). Provado por `first-run.test.tsx:826` em diante — "draws one result line per configured thing, each unchecked until asked", "checks one at a time and lets that one be tried again" (retry usa o mesmo botão, rótulo muda para "Check it again"). |
| FR-005, cláusula "diagnóstico de até duas frases" | FEITO (novo) | `verify.tsx:71-96` (`DIAGNOSIS_SENTENCE_LIMIT = 2`, `capSentences`), aplicado em `verify.tsx:189-192,232-238`. Constante local de apresentação, não de `config/constants/` — não é um limite que o console manda ao backend nem um orçamento que ele impõe; é puramente quanto texto uma linha mostra antes de recolher o resto, o mesmo estatuto de `FEED_LENGTH` em `dashboard.tsx`. Vermelho visto: "shows at most two sentences of a failure, with the rest one press away" (`first-run.test.tsx:915-931`) falhou (elemento `verify-detail` continha a 3ª frase) antes da implementação. |
| FR-005, cláusula "ação que aterrissa no campo que corrige (contrato de CTA)" | FEITO (novo) | `verify.tsx:98-106` (`fixOf`) — `provider` → `hrefFor('model')`, `integration` → `hrefFor('integrations')`, rotulados por `labels.fixProvider`/`labels.fixIntegration`. Renderizado em `verify.tsx:226-229` (`data-testid="verify-fix"`), só quando `verdict.state === 'failed'` (não para `unreachable`, que não tem campo a culpar). Vermelho visto: "lands a failed model provider on the field that corrects it..." e "lands a failed integration on its own step..." (`first-run.test.tsx:882-912`) antes da implementação. Destino do provider é `hrefFor('model')` (via `import { hrefFor } from './plan'`), não uma string literal — muda num lugar só quando a 040 existir. |
| FR-005, cláusula "diagnóstico completo permanece acessível em expansão" | FEITO (novo) | `verify.tsx:8,239-246` — `Reference` (`console/src/design/reference.tsx`, já existente da 001) envolvendo `capped.overflow`. Vermelho visto no mesmo teste "shows at most two sentences..." (a asserção sobre `reference`/expansão). "offers no expansion when a short failure has nothing left to hide" (`first-run.test.tsx:933-940`) prova que uma falha curta não ganha um `Reference` vazio. |
| FR-006, cláusula "'Continuar mesmo assim' DEVE existir para falhas não bloqueantes" | FEITO (novo) | `first-run.tsx:662-693` (`verify-continue-anyway`, `continue-anyway`), condicionado a `verifiable.length > 0 && !stepDone('verify', setup) && !stepBlocking('verify', setup)`. Navega para `hrefFor(nextStep('verify') ?? 'estate')` = `/first-run?step=estate`. Vermelho visto: "offers to continue past a provider that is stored but unverified..." (`first-run.test.tsx:1080-1104` região) falhou (`getByTestId('continue-anyway')` não encontrado) antes da implementação. Provado ao vivo contra o cenário `first-run` em `first-day.spec.ts` originalmente, mas **removido dali** — ver "O que fica pendente" abaixo. |
| FR-006, cláusula "o que é bloqueante... é declarado por passo" | FEITO (novo) | `console/src/surfaces/first-run/plan.ts:225-241` (`stepBlocking`) — `verify` bloqueia sse `setup.provider === 'absent'`; nenhum outro passo bloqueia hoje. A decisão (provider **ausente** bloqueia; provider **presente mas falhando a verificação** — o próprio exemplo do mockup, gemini-2.5-flash sem tool calling — não bloqueia) está documentada no docstring da função. Vermelho visto em `console/tests/unit/surfaces/first-run-plan.test.ts:235-254` ("what is blocking, declared per step") — `stepBlocking is not a function` — antes da implementação. |
| US2 AC4, segunda oração "com a pendência anotada no dashboard" | FEITO (já existia, verificado — sem novo teste dedicado) | O dashboard (`SetupHero`, `console/src/surfaces/setup-hero.tsx:41-46`) lê `outstanding(setup)`/`planFor(setup)` do mesmo `setup.steps` que `GET /v1/setup/checklist` serve. O backend só marca o passo `model-provider` como `state: "done"` depois que a verificação passa de verdade (confirmado em `fixtures/scenarios/populated/setup-checklist.json:36-37`, `detail: "a live request reached the endpoint and called a tool"`, contra `fixtures/scenarios/first-run/setup-checklist.json:26-29`, `state: "ready"` enquanto não verificado) — logo uma verificação pendente permanece "outstanding" no dashboard por construção da fonte única, sem eu precisar escrever nada novo. **Não escrevi um teste dedicado a esta cláusula especificamente** porque provaria de novo a mesma fiação de fonte única que `first-run-plan.test.ts:211` ("counts what the deployment itself reports as not done, never a client tally") e os testes existentes de `SetupHero` já provam; ver "O que fica pendente" para o porquê de eu não ter conseguido demonstrar isso ao vivo via Playwright. |
| FR-007, cláusula "resumir o que ficou configurado (nomes de exibição, estados)" | FEITO (novo) | `first-run.tsx:769-784` (`data-testid="setup-complete-summary"`), reaproveitando a lista `established` (`first-run.tsx:291-315`, já existente) dentro do bloco `setup-complete`. Vermelho visto: "resumes what got configured, by display name and canonical state..." (`first-run.test.tsx:1396-1408`) — `getByTestId('setup-complete-summary')` não encontrado — antes da implementação. |
| FR-007, cláusula "encerrar no dashboard" | FEITO (já existia, verificado) | `console/src/app/(shell)/first-run/page.tsx:28-33` — `if (setup.checklistComplete) redirect('/')`. Construído pela 010, commit `45d8f10` (`git log -p` confirma o diff). Provado ao vivo em `console/tests/e2e/setup-wizard.spec.ts:26-38` — visita a `/first-run` com o checklist completo (`populated`) redireciona a `/`, `page-header` mostra `data-area="dashboard"`, `setup-hero` ausente. |
| FR-007, cláusula "a rota passa a redirecionar" | FEITO (já existia, verificado) | Mesmo `first-run/page.tsx:28-33`. Este é o mecanismo que a CONFRONTO.md §2.3 já tinha citado indiretamente (a captura visual `first-run-1440-light` fotografa o dashboard); meu teste em `setup-wizard.spec.ts` é a primeira asserção *funcional* direta dela, não apenas visual. |
| FR-008 (substituir os três painéis antigos, nenhum estado duplicado) | FEITO — julgamento registrado | A tela já não tem "What is left / What is set up so far / Check that each of them works": tem o painel de passos (`first-run.tsx:415-464`), o painel `established` (`first-run.tsx:469-493`) e o corpo do passo atual (`first-run.tsx:497+`). **Julguei `established` como não duplicado** — seu próprio docstring (`first-run.tsx:426-428`, já existente) diz "'Ticked' and 'established' are different claims", e de fato: o stepper mede done/not-done por *passo da UI* (uma decisão composta, ex. provider+credential são um só fato), `established` mede *readiness canônica por credencial* (stored/verified/failing). São medidas diferentes, não o mesmo fato em dois vocabulários. **O duplicado real era a contagem do título** (FR-002), já corrigido. Não apaguei `established`. |

## 2. Ledger — Cenários de aceitação, por user story

| Peça | Estado | Detalhe |
|---|---|---|
| US1 AC1 (stepper, subtítulo) | FEITO | Ver FR-001 e a linha "Passo N de 7" acima. |
| US1 AC2 (retorno com trilha) | FEITO | Ver FR-003 acima. |
| US1 AC3 (mesma contagem em toda superfície) | FEITO | `outstanding`/`planFor`/`setup.steps.length` são a única fonte lida por `first-run.tsx`, `setup-hero.tsx`, `emptiness.ts:47`, `dashboard.tsx:275`(uso de `readSetup`), `shell/load.ts:273`. Nenhum desses consumidores foi alterado por mim além do conserto do título; `tests/contract/console/test_console_first_run.py::test_setup_state_is_read_from_the_checklist_route_and_nowhere_else` (parametrizado por screen/dashboard/shell) continua verde. |
| US2 AC1 ("Stored", não "Nobody has checked") | FEITO (já existia, verificado com nova asserção) | `verify.tsx:196-201` passa `thing.readiness` (`'configured'`) direto a `StatusChip`; `credentialStatus('configured')` → `'stored'` (`console/src/design/status.ts:145`) → rótulo `status.credential.stored` = "Stored" (`en.ts:355`). Confirmado com a nova asserção `first-run.test.tsx:832-841` ("reads the canonical 'Stored', never a sentence about nobody having checked yet"), que passou sem precisar mudar produção. |
| US2 AC2 (diagnóstico curto + ação nomeada) | FEITO | Ver FR-005 acima. |
| US2 AC3 (chip vira "Verified" com tempo de resposta) | FEITO (já existia, verificado) | `verify.tsx:150-157` (`verdict.state === 'passed' ? 'verified' : 'failing'`); o tempo de resposta é o próprio `detail` que o backend devolve (ex. "Respondeu em 240 ms" no mockup) — passa por `capped.visible` sem edição, dentro do limite de 2 frases. |
| US2 AC4, primeira oração ("Continuar mesmo assim" disponível) | FEITO | Ver FR-006 acima. |
| US2 AC4, segunda oração (pendência no dashboard) | FEITO (já existia, verificado) | Ver linha dedicada acima. |
| US3 AC1 (wizard fecha → dashboard sem convite; rota redireciona) | FEITO (já existia, verificado) | Ver FR-007 acima. |
| US3 AC2 (passo desfeito → convite volta apontando o passo) | FEITO (já existia, verificado) | `SetupHero` (`setup-hero.tsx:41-46`) é um componente de servidor sem estado de sessão — cada render lê `outstanding(setup)`/`currentStep(setup)` de novo; não há nada para "lembrar" de um passo desfeito, o que é exatamente a propriedade pedida. Provado indiretamente por `first-run.test.tsx:1575` ("the dashboard of a deployment that is not set up" / "drops the plan and the warning once the deployment is set up", que exercita as duas direções via troca de cenário) — não escrevi um teste novo específico para "desfazer", porque a implementação não distingue "nunca fez" de "desfez"; ambas são "outstanding > 0" na leitura seguinte. |

## 3. Edge cases

| Peça | Estado | Detalhe |
|---|---|---|
| Gateway indisponível no meio do wizard | FEITO (novo teste; degradação em si já existia) | `console/tests/unit/surfaces/first-run.test.tsx:238-268` ("a gateway that cannot answer the checklist mid-wizard"). A infraestrutura de degradação (`Panel`/`panelRead`/`stateOf`) é da 001 e eu não a toquei; o que era novo era provar que o **wizard especificamente** preserva a posição do operador. Achado real ao escrever este teste: quando `here !== 'provider'`, o painel do *corpo do passo atual* também lê `checklist` (`first-run.tsx:` a expressão `stateOf(here === 'provider' ? providers : checklist, false)`, já existente, não alterada por mim) — então ele **também** entra em erro numa falha do checklist, e `wizard-body` não renderiza nesse caso. A prova de "preserva onde o operador está" é `wizard-position` (que não depende de nenhum `Panel`), não `wizard-body`. Vermelho visto: minha primeira versão do teste (que esperava `wizard-body` com `data-step="verify"`) falhou porque o elemento não existe nesse estado — troquei a asserção para o que de fato é verdade, e o teste passou. |
| Dois operadores no wizard ao mesmo tempo | FEITO (já existia, garantido por construção — não testado por mim) | Não há nenhum estado de wizard em cookie, `localStorage` ou React state — `currentStep`/`planFor`/`outstanding` são funções puras do que `readSetup` extrai da resposta do servidor a cada render. Já provado por `first-day.spec.ts:189-236` ("killing the browser mid-flow loses nothing"). Não escrevi um teste com dois `BrowserContext` simultâneos — seria a mesma propriedade provada de outro ângulo, e o teste existente já a cobre. |
| Ids crus no resumo | FEITO (já existia, verificado) | `established`/`setup-complete-summary` usam `entry.displayName` (`first-run.tsx:293-306,769-784`), nunca `entry.name`. Provado por `first-run.test.tsx:930-975` ("lists a credential once even when it is both the chosen provider and a configured integration") e pela nova asserção em `:1404` (`not.toHaveTextContent('anthropic')`). |

## 4. Success Criteria

| Peça | Estado | Detalhe |
|---|---|---|
| SC-001 (completa o setup sem abandonar o funil) | Não verificável por este agente | É um critério de teste de usabilidade humana ("teste de usabilidade" no próprio texto da spec), não uma propriedade que um teste automatizado demonstra. O que os testes automatizados desta feature demonstram é que o funil **pode** ser percorrido sem sair do shell (`first-day.spec.ts`, teste "the guided run walks provider to verification without leaving the shell", já existente) e que a trilha de volta agora existe (FR-003) — mas "sem abandonar" no sentido comportamental humano fica fora do que testei. |
| SC-002 (uma única contagem em todas as superfícies) | FEITO | Ver FR-002 e US1 AC3. |
| SC-003 (falha → correção: 1 clique; retorno → re-teste: 1 clique) | FEITO, com uma correção de redação feita pelo orquestrador | Falha → correção: `verify-fix` é um `Link` direto (`verify.tsx:227-229`), 1 clique. Retorno → re-teste: **um clique depois de voltar ao passo `verify`**, onde `VerifyStep` renderiza um botão "Check it"/"Check it again" por linha (`verify.tsx:203-224`). A redação original desta célula dizia que a tela de destino (`hrefFor('model')`/`hrefFor('integrations')`) "já é o passo com o botão pronto", o que não é verdade: aquele botão só renderiza quando `here === 'verify'`, e as telas de correção não o carregam. A propriedade se sustenta na leitura correta — voltando ao `verify`, o re-teste é um clique — mas a frase anterior induzia ao erro. Achado do verificador independente. |
| SC-004 (zero vocabulário fora do canônico) | FEITO (já existia, verificado) | `console/tests/e2e/vocabulary.spec.ts:88` ("the guided first run never shows a forbidden phrase") e `:97` ("what the first run has established shows canonical chips, not raw words") passaram na rodada completa do projeto `behaviour` (98 passed, ver §6). Não modifiquei este arquivo. |

## 5. Tasks T001–T013

| Task | Estado | Detalhe |
|---|---|---|
| T001 (fixtures em três estados) | FEITO — sem novo arquivo de fixture | `fixtures/scenarios/empty/setup-checklist.json`, `fixtures/scenarios/first-run/setup-checklist.json` e `fixtures/scenarios/populated/setup-checklist.json` já cobrem limpo/quase-limpo e completo. Não existe um cenário "meio" committado (nenhum dos três tem um passo feito e outro não simultaneamente) — mas o padrão já estabelecido no próprio arquivo de teste (`serveReadyForFirstInvestigation`, `serveRuntimeStep`, ambos já existentes em `first-run.test.tsx` antes desta feature) é construir esse estado **inline**, via `vi.stubGlobal('fetch', ...)` sobrepondo `/v1/setup/checklist`, exatamente o que os testes novos que escrevi fazem. Para Playwright, o estado "meio" é alcançado **dirigindo** o wizard a partir do cenário `first-run` (que é como o próprio `first-day.spec.ts` já fazia antes de mim). Não criei fixture nova porque nenhuma das minhas necessidades exigiu um documento committado que os três já não servissem — `test_only_the_builder_writes_a_fixture_file` (arquitetura) continua verde. |
| T002 (teste de contrato estendido, vermelho primeiro) | FEITO | `tests/contract/console/test_console_first_run.py:206-224`. Vermelho visto e citado na saída do pytest antes do conserto de FR-002 (ver §7). |
| T003 (modelo de passos estendido) | FEITO | `plan.ts:225-247` (`stepBlocking`, `nextStep`) — **não criei `steps.ts`**, estendi o módulo existente, como a task pede explicitamente. |
| T004 (teste behaviour, vermelho primeiro) | FEITO — dividido entre dois arquivos, ver nota abaixo | `console/tests/e2e/setup-wizard.spec.ts` (novo) cobre o que é verdadeiro contra o cenário `populated` (padrão do projeto `behaviour`): redirecionamento da rota quando completo, ausência do banner quando completo. `console/tests/first-day/first-day.spec.ts:260-296` (estendido) cobre o que precisa de checklist incompleto: posição do subtítulo, banner de retorno presente e o ciclo completo de ida-e-volta. Ver §7 para o porquê da divisão. |
| T005 (implementar stepper) | FEITO | Ver FR-001, US1 AC1. |
| T006 (banner de retorno) | FEITO | Ver FR-003. |
| T007 (teste behaviour de verificação, vermelho primeiro) | FEITO — no nível de componente, não ao vivo | `first-run.test.tsx:882-940` (região "verifying what is configured"), reproduzindo o texto exato do caso do mockup ("gemini-2.5-flash answered without calling the tool"). Ver §7 para por que a reprodução **ao vivo** (Playwright) deste caso específico não foi feita. |
| T008 (implementar verificação com CTA/expansão) | FEITO | Ver FR-005. |
| T009 ("Continuar mesmo assim" + bloqueio, com teste) | FEITO | Ver FR-006. |
| T010 (teste US3, vermelho primeiro) | FEITO | `console/tests/e2e/setup-wizard.spec.ts:26-48` (rota redireciona quando completo). "Passo desfeito ⇒ convite volta" não ganhou um teste Playwright dedicado — ver US3 AC2 acima para o porquê (a implementação não distingue "nunca feito" de "desfeito"; ambos os casos já são a mesma propriedade "outstanding > 0", provada em vitest). |
| T011 (implementar conclusão + redirecionamento) | FEITO — redirecionamento já existia, resumo é novo | Ver FR-007. |
| T012 (edge: gateway indisponível) | FEITO | Ver edge cases acima. |
| T013 (varredura de vocabulário, `make verify`, `controle.md`) | FEITO — com uma ressalva sobre `make verify` em si, ver §6 | Varredura: `vocabulary.spec.ts` (já existente) passou contra o wizard alterado. Gates: ver §6. `controle.md`: este arquivo. |

## 6. Gates rodados, e o resultado real

- **`uv run python -m tools.console_gate typecheck`** — PASSOU. `tsc --noEmit`, sem saída (sucesso).
- **`uv run python -m tools.console_gate lint`** — PASSOU. `eslint .` + `node scripts/check-css-literals.mjs`, sem saída.
- **`uv run python -m tools.console_gate format-check`** — FALHOU na primeira rodada (5 arquivos com formatação divergente, todos meus); corrigido com `pnpm exec prettier --write` nos 5 arquivos; **PASSOU** na segunda rodada.
- **`uv run python -m tools.console_gate build`** — FALHOU na primeira rodada: `Property 'className' does not exist on type 'IntrinsicAttributes & LinkProps'` em três lugares (`return-banner.tsx:66`, `verify.tsx:230`, `first-run.tsx:683`) — eu tinha passado `className` para o componente `Link` (`console/src/components/action.tsx:154-160`), que não aceita essa prop e já aplica seu próprio estilo fixo. Corrigido removendo `className` das três chamadas; **PASSOU** na segunda rodada (build de produção completo, standalone gerado).
- **`uv run python -m tools.console_gate test`** (vitest + cobertura) — PASSOU. **2042 testes, 128 arquivos, 0 falhas.** Cobertura: Statements 93.98%, Branches 90.16%, Functions 91.36%, Lines 96.02% — todas acima do piso de 90 citado no baseline da onda.
- **`python -m tools.console_e2e run --project first-day --scenario first-run -- tests/first-day/first-day.spec.ts`** — PASSOU depois do rebuild (ver acima). **7 passed** (5 já existentes + 2 novos meus). Antes do rebuild, 3 falharam por estarem rodando contra o `.next/standalone` obsoleto (`wizard-position` inexistente, `handover-link` sem `?return=setup`) — não eram falhas de lógica, eram build desatualizado; corrigidas junto com o `build`.
- **`python -m tools.console_e2e run --project behaviour -- tests/e2e/setup-wizard.spec.ts`** — PASSOU. **2 passed** (arquivo novo).
- **`python -m tools.console_e2e run --project behaviour`** (suíte behaviour completa, para garantir que `resources.tsx`/`settings-not-built.tsx`/`first-run.tsx` não quebraram nada fora do meu escopo direto) — PASSOU. **98 passed, 6 skipped** (os 6 já eram skipped antes de mim, retirados pela 010 — commit `45d8f10`, "Six browser tests covering the intake and delivery panes are skipped rather than deleted").
- **`make lint`** (ruff sobre o Python) — PASSOU. "All checks passed!"
- **`make typecheck`** (mypy) — PASSOU. "Success: no issues found in 1779 source files."
- **`make check-imports`** — PASSOU. 7 contratos mantidos, 0 quebrados, 1672 arquivos.
- **`make check-constants`** — PASSOU (sem saída).
- **`make check-protocols`** — PASSOU (sem saída).
- **`make check-deps`** — PASSOU (sem saída).
- **`make check-console-boundary`** — PASSOU (sem saída).
- **`uv run pytest -q`** (suíte completa, isolada — ver §7 sobre a primeira tentativa) — **1 failed, 14057 passed, 25 skipped** em 613.72s. A única falha é `tests/contract/console/test_console_visual_regression.py::test_the_untouched_baselines_still_match`, exatamente a esperada e documentada como estado permanente enquanto os 23 baselines visuais continuam em aberto por decisão do operador (`specs_v5/CONFRONTO.md` §2.3). Nenhuma outra falha.
- **`make verify`** — **NÃO RODADO por inteiro.** Conforme o próprio briefing, `make verify` termina EXIT=2 na etapa visual (estado permanente, não desta feature) e nunca chega ao pytest; rodar `make verify` de ponta a ponta não teria me dado sinal adicional além do que os gates acima já deram separadamente, e o próprio briefing pede que essa etapa fique para o orquestrador. Rodei cada peça de `make verify` que se aplica ao que toquei, individualmente, listada acima.

## 7. Incidentes durante a execução, registrados sem esconder

- **Rodei dois `pytest` ao mesmo tempo.** Ao verificar `tests/contract/console/` isoladamente enquanto uma rodada da suíte completa (`uv run pytest -q`, lançada via `nohup ... &` em background) ainda estava viva, os dois processos coexistiram por um período. O resultado da rodada concorrente mostrou 5 falhas em `tests/contract/console/` (4 em `test_console_gate.py`, que semeia `seeded.*`, mais a esperada), e a rodada completa concorrente mostrou uma falha adicional em `test_console_design_system.py`. **Verifiquei a árvore imediatamente após ambos os processos terminarem: `git status` limpo (nenhum `.png` de `console/visual/` tocado, nenhum `seeded.*` órfão).** Rodei os dois conjuntos de testes suspeitos **isoladamente, sem nenhuma concorrência**, depois: `test_console_design_system.py` e `test_one_fictional_deployment.py` passaram limpos (31 passed) assim que limpei os artefatos residuais de `console/test-results/`/`console/playwright-report/` de uma rodada Playwright anterior — exatamente o artefato conhecido que o briefing descreve. Rodei a suíte completa uma segunda vez, inteiramente isolada (nenhum outro pytest, nenhum vitest, nenhum comando concorrente), e ela terminou com **1 failed, 14057 passed, 25 skipped** — a única falha sendo a esperada. As 5 falhas da rodada concorrente foram, portanto, artefato da minha própria colisão, não um defeito real; a rodada isolada é a que vale, e é a que está registrada no §6.
- **O `.next/standalone` usado por `console_e2e` não é rebuildado automaticamente.** Rodei os testes Playwright novos antes de rodar `console_gate build`, e eles falharam contra um binário obsoleto (pré-minhas-mudanças) sem nenhum aviso — o `wizard-position` simplesmente não existia no HTML servido, e `handover-link` não carregava `?return=setup`, ambos porque o servidor standalone era de uma versão anterior do código. Descobri isso ao notar que meus testes de componente (vitest, que importam o módulo fonte diretamente) já provavam a mesma lógica como verde. Rodar `console_gate build` antes de qualquer `console_e2e run` resolveu — e revelou, de quebra, o erro real de `className` no `Link` (ver §6).

## 8. O que fica pendente, nomeado, não escondido

- **A reprodução ao vivo (Playwright) do caso "gemini-2.5-flash sem tool calling" não foi feita.** `provider-verify.json` do cenário `first-run` (`fixtures/scenarios/first-run/provider-verify.json:6-11`) é uma resposta canônica fixa (`verified: true`, `provider_id: 'anthropic'`) que o MockPlane serve **sem distinguir por provider** — não há como, através de uma requisição real do navegador contra este mock, obter uma falha de verificação com o texto do mockup. A cobertura de FR-005/T007 para o *conteúdo* do diagnóstico, o CTA e a expansão está inteiramente em `console/tests/unit/surfaces/first-run.test.tsx` (componente, com `fetch` mockado — o mesmo padrão que os testes *já existentes* deste arquivo usavam antes de mim para o mesmo tipo de caso). Quem quiser a prova ao vivo precisa ou (a) estender o builder do MockPlane (`tools/mockplane/dataset/build.py`) para servir uma resposta de `provider-verify` condicionada ao provider pedido, ou (b) provar contra um deployment real, como `tests/contract/console/test_console_first_run.py::test_a_credential_written_the_way_the_console_writes_it_verifies` já faz para o caso de integração. Nenhuma das duas é minha para decidir sozinho — é uma limitação da ferramenta de mock, não desta feature. Dono: quem manutém `tools/mockplane/`.
- **T009's teste comportamental de "Continuar mesmo assim" contra um cenário ao vivo foi escrito e depois removido.** Descobri, ao rodá-lo, que `checklist_record` (`tools/mockplane/dataset/served.py:1662`) é uma função auxiliar do **builder** de fixtures, não algo recomputado a cada requisição — `/v1/setup/checklist` no MockPlane é um documento estático por cenário durante toda uma rodada Playwright, então armazenar uma credencial ao vivo (`PUT /v1/integrations/{name}/credential`, que responde 200 de verdade) **não** move `provider`/`readiness` na próxima leitura do checklist. Isso é diferente do gateway real, que move de verdade (provado por `test_a_credential_written_the_way_the_console_writes_it_verifies`). O comentário deixado em `first-day.spec.ts` (onde o teste removido estava) documenta isso. Dono: mesmo módulo do item anterior.
- **SC-001 (teste de usabilidade)** não é verificável por mim — é explicitamente um teste de usabilidade humana no texto da própria spec. Dono: quem conduzir esse teste, fora do escopo de um agente.
- **"Dois operadores simultâneos" (edge case)** não ganhou um teste com dois `BrowserContext` Playwright concorrentes — a propriedade que o tornaria verdadeiro (nenhum estado de sessão no browser) já é provada por `first-day.spec.ts`'s teste existente "killing the browser mid-flow loses nothing", e um teste com dois contextos provaria exatamente a mesma coisa de um ângulo mais caro de construir. Deixei por redundância de custo, não por dúvida sobre o comportamento.
- **Não toquei `/settings/models-providers` nem `/settings/alert-intake`'s conteúdo real** — ambos continuam sendo `NotBuiltSettingsPage` (o estado não-construído compartilhado), como pedido no escopo ("não construa as telas da 040/050/060"). O que toquei em `settings-not-built.tsx` foi só o banner de retorno, que é genérico às 5 páginas não construídas e só ativa com `?return=setup` explícito — nenhuma das outras 4 páginas ganha comportamento novo na prática, porque nada as linka com esse parâmetro.
- **A mistranslation `dashboard.hero.next: 'Seguinte'` (pt-BR) e `firstRun.estate.found`'s "Nada foi gravado"** foram corrigidas (`'Próximo'`, "Nada foi guardado") como achado adicional fora do ledger desta spec — "Seguinte" é europeísmo claro para "next", e "gravado" destoava do resto do próprio arquivo, que usa "guardado" para o mesmo conceito em toda parte. Ambas em `console/src/i18n/pt-BR.ts`. Não são cobertas por nenhum teste automatizado que eu tenha visto (nenhum teste afirma o texto pt-BR exato dessas duas chaves); a correção foi validada rodando `tests/unit/surfaces/dashboard.test.tsx` e `first-run.test.tsx` de novo (136/136... 104/104 passed) para confirmar que nada dependia do texto antigo.

## 9. Onde a especificação, o plano ou o mockup não responderam, e o que assumi

- **Onde a "correção de modelo" aterrissa antes da 040 existir**: usei `hrefFor('model')` (não uma string literal), exatamente como o briefing pediu.
- **Se `HANDOVER` nomeia o endereço final ou atravessa o redirect**: nomeia o final (`/settings/alert-intake`), decisão e motivo registrados na linha de FR-003 acima.
- **O que fazer com a contagem dupla**: o título passou a ler a fonte única (não removi a contagem do título — o título nunca teve um *número* nele, só a escolha entre dois rótulos fixos; o que era duplicado era a base da escolha, não um número visível duas vezes).
- **CTA de uma falha de integração** (o mockup só mostra o caso de um provider): assumi `hrefFor('integrations')` como destino — a spec e o mockup não cobrem este caso, e o step "integrations" é onde todas as credenciais de integração são geridas, então é o campo mais honesto disponível hoje sem uma tela por-integração dedicada.
- **A ordenação dos painéis do FR-008**: assumi que "nenhum estado duplicado permanece" já valia estruturalmente (fora a contagem do título), e não reescrevi o layout de dois-painéis-mais-corpo em um único cartão como o mockup desenha — o mockup é normativo para vocabulário e hierarquia, não pixel a pixel, e essa reestruturação maior não foi pedida por nenhum FR específico.
