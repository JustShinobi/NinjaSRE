"""The fictional deployment: one estate, one history, two consumers.

The mock API serves this to the console; the demo seeder loads the same records
into a real database. Two datasets would drift, and the first person to notice
would be somebody whose demo looked nothing like the screenshots.
"""

from __future__ import annotations

__all__: list[str] = []
