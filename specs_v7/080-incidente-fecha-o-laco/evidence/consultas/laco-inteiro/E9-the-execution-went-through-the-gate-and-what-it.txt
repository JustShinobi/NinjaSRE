# E9 — the execution went through the gate, and what it produced was recorded
# collected at 2026-08-25T07:35:01.956155+00:00
# parameters: org=default run=d393d5d0916c404b854337a270fa825d incident=inc_05328c58ca88676b approval=(none) resource=res-47555daca81efa67dacce16fb42c898e

## outcome — the execution produced a recorded outcome for this resource — a named failure is an outcome, a silent one is not
## before the wave: no row

-- A named failure is an outcome and closes the loop negatively. What does not
-- close the loop is no row at all.
SELECT action_id,
       capability,
       resource_id,
       run_id,
       incident_id,
       state,
       verdict,
       rollback,
       autonomous,
       executed_at,
       due_at,
       verified_at,
       left(detail, 200) AS detail
FROM remediation_outcomes
WHERE org_id = 'default'
  AND resource_id = 'res-47555daca81efa67dacce16fb42c898e'
ORDER BY executed_at DESC;

--- output ---
 action_id | capability | resource_id | run_id | incident_id | state | verdict | rollback | autonomous | executed_at | due_at | verified_at | detail 
-----------+------------+-------------+--------+-------------+-------+---------+----------+------------+-------------+--------+-------------+--------
(0 rows)


--- end of output ---


## episode — what a later investigation would be told about this one
## before the wave: no row for a run

SELECT episode_id,
       title,
       outcome,
       occurred_at,
       components
FROM episodes
WHERE org_id = 'default'
  AND run_id = 'd393d5d0916c404b854337a270fa825d';

--- output ---
 episode_id | title | outcome | occurred_at | components 
------------+-------+---------+-------------+------------
(0 rows)


--- end of output ---

