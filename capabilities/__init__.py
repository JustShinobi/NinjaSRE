"""Tier 2 — the agent-callable surface.

Typed tools, methodology skills, the registry, discovery, scoring, and bounded
selection. May import ``integrations``, ``core``, ``platform``, and ``config``;
never a tier 1 package.

Agent-callable tools live here, not in the repository's root ``tools/``
directory, which holds repository tooling only.
"""

from __future__ import annotations
