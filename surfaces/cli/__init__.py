"""The ``ninjasre`` command: a scriptable CLI over the same core every surface drives.

``app`` is the typer application root; ``client`` is what a command talks to,
local or remote; ``output`` decides what the destination can render and renders
it; ``wizard`` is the guided first run.
"""

from __future__ import annotations
